server:
  port: 8081

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BJStudio25/BasicMSA-ConfigServer

eureka:
  client:
    region: default
    registry-fetch-interval-seconds: 5
    service-url:
      default-zone: http://localhost:8082/eureka/
      